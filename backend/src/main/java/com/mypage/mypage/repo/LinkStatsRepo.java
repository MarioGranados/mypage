package com.mypage.mypage.repo;

import com.mypage.mypage.models.LinkStats;
import org.springframework.data.jpa.repository.JpaRepository;

public interface LinkStatsRepo extends JpaRepository<LinkStats, Long> {
}
